package server

import (
	"testing"
)

func TestConsumer(t *testing.T) {
	// 	consumer := NewGoatQConsumer(ConsumerOpts{Addr: ":3000"})
	// 	go consumer.Consume()
	// 	for i := 0; i < 5; i++ {
	// 		consumer.messageChannel <- []byte(fmt.Sprintf("msg%d", i))
	// 	}
}
